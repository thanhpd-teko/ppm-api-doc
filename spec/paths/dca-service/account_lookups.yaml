get:
  summary: "Filter Account"
  tags:
    - Account Lookup
  parameters:
    - name: taxCode
      in: query
      schema:
        type: string
    - name: telephone
      in: query
      schema:
        type: string
    - name: iamId
      in: query
      schema:
        type: string
    - name: page
      in: query
      schema:
        type: integer
      required: true
    - name: pageSize
      in: query
      schema:
        type: integer
      required: true
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              code:
                type: string
              message:
                type: string
              page_response:
                type: object
                properties:
                  current_page:
                    type: integer
                  total_page:
                    type: integer
                  total_items:
                    type: integer
                  page_size:
                    type: integer
              data:
                type: object
                properties:
                  documents:
                    type: array
                    items:
                      $ref: "#/components/schemas/account"

