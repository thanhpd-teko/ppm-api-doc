get:
  description: ':return: list[Location]'
  operationId: get_location_mapping
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/location_mapping_res'
  summary: Get list all locations mapping
  tags:
    - locations
