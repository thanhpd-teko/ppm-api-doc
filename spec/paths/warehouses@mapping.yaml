get:
  description: ':return: list[Warehouse]'
  operationId: get_warehouse_mapping
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/warehouse_mapping_res'
  summary: Get list all warehouses mapping
  tags:
    - warehouses
