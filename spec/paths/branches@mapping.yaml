get:
  description: ':return: list[Branch]'
  operationId: get_branch_mapping
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/branch_list_res'
  summary: Get list all branches mapping
  tags:
    - branches
