get:
  description: ':return: list[Branch]'
  operationId: get_branch_mapping
  tags:
    - branches
  parameters:
    - name: X-Fields
      format: mask
      type: string
      description: An optional fields mask
      in: header
  summary: Get list all branches mapping
  responses:
    '200':
      description: Success
      schema:
        items:
          $ref: '#/definitions/branch_list_res'
        type: array
