get:
  responses:
    '200':
      description: Successful Return
      content:
        application/json:
          schema:
            items:
              $ref: '#/components/schemas/_stock_quant_item_min'
            type: array
  parameters:
    - name: products
      description: List sku
      required: true
      in: query
      explode: true
      schema:
        type: array
        items:
          type: integer
    - name: regions
      description: Regions
      in: query
      explode: true
      schema:
        type: array
        items:
          type: integer
    - name: branches
      description: Branches
      in: query
      explode: true
      schema:
        type: array
        items:
          type: string
    - name: warehouses
      description: Warehouses code
      in: query
      explode: true
      schema:
        type: array
        items:
          type: string
    - name: locations
      description: Locations
      in: query
      explode: true
      schema:
        type: array
        items:
          type: string
  description: ':return: list[StockQuant]'
  operationId: get_stock_quants_out
  tags:
    - Stock Quantities
  summary: Get list all stock quants but for minimize properties
