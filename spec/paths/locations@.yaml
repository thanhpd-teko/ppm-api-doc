get:
  description: ':return: list[Location]'
  operationId: get_locations
  tags:
    - locations
  parameters:
    - name: X-Fields
      format: mask
      type: string
      description: An optional fields mask
      in: header
  summary: Get list all locations
  responses:
    '200':
      description: Successful Return
      schema:
        items:
          $ref: '#/definitions/location_list_res'
        type: array
