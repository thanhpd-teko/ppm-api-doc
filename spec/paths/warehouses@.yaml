get:
  description: ':return: list[Warehouse]'
  operationId: get_warehouses
  tags:
    - warehouses
  parameters:
    - name: X-Fields
      format: mask
      type: string
      description: An optional fields mask
      in: header
  summary: Get list all warehouses
  responses:
    '200':
      description: Successful Return
      schema:
        items:
          $ref: '#/definitions/warehouse_list_res'
        type: array
