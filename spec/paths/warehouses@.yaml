get:
  summary: Get list all warehouses
  responses:
    '200':
      description: Successful Return
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/warehouse_list_res'
  operationId: get_warehouses
  description: ':return: list[Warehouse]'
  parameters:
    - name: X-Fields
      in: header
      description: An optional fields mask
      schema:
        type: string
        format: mask
  tags:
    - warehouses
