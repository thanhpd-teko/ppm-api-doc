parameters:
  - name: id
    description: ID phiếu Pick của stock.picking cần xử lý WMS
    required: true
    in: path
    schema:
      type: integer
put:
  requestBody:
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/eton_so_returned_req'
    required: true
  responses:
    '200':
      description: Successful updating
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/eton_success_res'
    '500':
      description: Operation failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/eton_fail_res'
  tags:
    - Eton
  operationId: put_external_so_return
