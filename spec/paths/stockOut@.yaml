post:
  description: ':return:'
  operationId: post_stock_out
  tags:
    - stockOut
  parameters:
    - name: payload
      required: true
      schema:
        $ref: '#/definitions/stock_out_request_req'
      in: body
  summary: Yêu cầu lấy hàng
  responses:
    '200':
      description: Xác nhận yêu cầu lấy hàng
    '403':
      description: Đã hủy đơn
put:
  description: ':return:'
  operationId: put_stock_out
  tags:
    - stockOut
  parameters:
    - name: payload
      required: true
      schema:
        $ref: '#/definitions/stock_out_confirm_req'
      in: body
  summary: Xác nhận lấy hàng
  responses:
    '200':
      description: Xác nhận yêu cầu lấy hàng
    '403':
      description: Hàng thiếu
