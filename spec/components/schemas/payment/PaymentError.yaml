required:
  - code
  - message
properties:
  code:
    type: integer
    example: 400
  message:
    type: string
    example: 'IP is not allowed to connect'   
  error:
    $ref: '#/components/schemas/PaymentBusinessError'    
