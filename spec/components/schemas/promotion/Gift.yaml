properties:
  sku:
    type: string
  orderLineId:
    type: integer
    description: Id của order line converted từ gift này
  quantity:
    type: integer
  name:
    type: string
required:
  - sku
  - quantity
  - name
  - orderLineId
