required:
  - code
properties:
  code:
    type: string
    description: Mã trả về, thành công nếu code==success.
  messsage:
    type: string
  result:
    required:
      - transaction
    properties:
      transaction:
        properties:
          id:
            type: integer
            description: Mã giao dịch ngân sách
        required:
          - id
