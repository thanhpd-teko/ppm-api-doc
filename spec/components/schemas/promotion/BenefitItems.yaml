required:
  - sku
  - name
  - price
  - total
properties:
  id:
    type: integer
  applyOn:
    type: string
    enum:
      - product
      - order
  sku:
    type: string
  quantity:
    type: integer
  name:
    type: string
  budgetStatus:
    $ref: "#/components/schemas/BudgetStatus"
  budgetId:
    type: integer
    description: Id của ngân sách
  outOfBudget:
    type: boolean
    description: Ngân sách còn hay hết
