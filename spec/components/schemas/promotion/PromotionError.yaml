required:
  - code
  - message
properties:
  code:
    type: string
    description: error code
    example: 01
  message:
    type: string
    description: error message
    example: Invalid order promotion!
  extra:
    properties:
      orderPromotions:
        properties:
          promotions:
            type: array
            items:
              properties:
                id:
                  type: integer
                  description: Promotion id
                errorCode:
                  type: string
                errorMessage:
                  type: string
            description: List các ctkm của đơn hàng bị lỗi
          errorCode:
            type: string
          errorMessage:
            type: string
      productPromotions:
        type: array
        items:
          properties:
            sku:
              type: string
            quantity:
              type: integer
            errorCode:
              type: string
            errorMessage:
              type: string
            promotions:
              type: array
              items:
                properties:
                  id:
                    type: integer
                  errorCode:
                    type: string
                  errorMessage:
                    type: string

