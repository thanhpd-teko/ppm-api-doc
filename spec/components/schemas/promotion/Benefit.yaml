properties:
  type:
    type: string
    enum:
      - discount
      - gifts
  discount:
    properties:
      # applyOn:
      #   type: string
      #   enum:
      #     - item
      #     - order
      type:
        type: string
        enum:
          - amount
          - percent
      value:
        type: number
        description: Gía trị giảm (tuỳ vào type)
      maxDiscount:
        type: number
        description: Giá trị giảm giá tối đa
  items:
    type: array
    items:
      $ref: "#/components/schemas/Gift"

